{
  "name": "luxury-hotel-system",
  "version": "1.0.0",
  "description": "Luxury Hotel & Resort Management System",
  "main": "index.js",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "npm install -g serve && serve -s frontend/dist -l $PORT",
    "heroku-postbuild": "npm run install-frontend && npm run build"
  },
  "keywords": [
    "hotel",
    "resort",
    "booking"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^14.2.1"
  }
}
